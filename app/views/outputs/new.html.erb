<div class="container mt-5">
  <h1 class="display-1">New text</h1>
  <div class="row" data-controller="realtime-fields">
    <div class="col-5">
    <%= simple_form_for [@template, @output], data: { turbo: false, controller: 'submit-loader', action: 'submit->submit-loader#submit' } do |f| %>
      <%= render "form", f: f, output: @output %>
    <%= render "shared/loading_hexes" %>
    <% end %>
    </div>

      <div class="col-7 card bg-light p-3 rounded fs-5">
        <%= content_with_fields example: @example, controller: 'realtime-fields', only_active: true  %>
      </div>
  </div>
</div>
